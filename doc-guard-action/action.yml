name: "Doctyze Doc Guard"
author: "Actyze"
description: "Documentation-coupling guard for pull requests. Plugs into any existing PR review agent (CodeRabbit, Greptile, Qodo, Copilot Code Review, etc.). Ensures the canonical doc layer is updated whenever code changes."
branding:
  icon: "book-open"
  color: "blue"

inputs:
  mode:
    description: "Enforcement mode: warn-only | block-required | block-all"
    required: false
    default: "warn-only"
  llm:
    description: "LLM provider: claude | openai | ollama"
    required: false
    default: "claude"
  anthropic-api-key:
    description: "Anthropic API key (when llm=claude)."
    required: false
  openai-api-key:
    description: "OpenAI API key (when llm=openai)."
    required: false
  config:
    description: "Path to .doctyze.yaml (default: .doctyze.yaml at repo root)."
    required: false
    default: ".doctyze.yaml"
  stack-profile:
    description: "Override stack profile: modern | legacy."
    required: false
  exclude-paths:
    description: "Comma-separated glob patterns to skip (e.g., 'vendor/**,test/**')."
    required: false

outputs:
  doc-warnings-count:
    description: "Number of documentation-drift warnings posted on the PR."
  required-doc-updates:
    description: "Number of required doc updates that block merge (only > 0 when mode != warn-only)."

runs:
  using: "composite"
  steps:
    - name: Check out code with history
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        ref: ${{ github.event.pull_request.head.sha }}

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - name: Install Doctyze
      shell: bash
      run: pip install actyze-doctyze[llm]

    - name: Run Doctyze doc-coupling analysis
      shell: bash
      env:
        ANTHROPIC_API_KEY: ${{ inputs.anthropic-api-key }}
        OPENAI_API_KEY: ${{ inputs.openai-api-key }}
        DOCTYZE_MODE: ${{ inputs.mode }}
        DOCTYZE_LLM: ${{ inputs.llm }}
        DOCTYZE_CONFIG: ${{ inputs.config }}
        DOCTYZE_STACK_PROFILE: ${{ inputs.stack-profile }}
        DOCTYZE_EXCLUDE_PATHS: ${{ inputs.exclude-paths }}
        GITHUB_TOKEN: ${{ github.token }}
      run: |
        doctyze doc-guard run \
          --pr-number "${{ github.event.pull_request.number }}" \
          --base-sha "${{ github.event.pull_request.base.sha }}" \
          --head-sha "${{ github.event.pull_request.head.sha }}"
