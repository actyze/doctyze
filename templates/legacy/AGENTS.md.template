# {{SERVICE_NAME}} — Agent context (Legacy stack)

> AGENTS.md / AAIF standard. Generated by [Doctyze](https://github.com/actyze/doctyze).
> Hand-edits are preserved.

## What this application does

{{APPLICATION_DESCRIPTION}}

Confidence: 🟡 INFERRED — verify and refine.

## Legacy stack profile

- **Primary language**: {{LEGACY_LANGUAGE}} ({{LANGUAGE_VARIANT}})
- **Runtime / platform**: {{PLATFORM}}  (e.g., z/OS, IBM i, SAP NetWeaver, Windows Server 2008)
- **Source-of-truth SCM**: {{SCM}}  (e.g., Endevor, ChangeMan, ARCAD, abapGit, VSS, git)
- **Build/transport mechanism**: {{BUILD_TRANSPORT}}  (e.g., JCL, SAP transport requests, IBM i job scheduler)
- **Database / DDL**: {{DATA_PLATFORM}}  (e.g., DB2 z/OS, IMS, IBM i DDS, Oracle, SAP DDIC)

## Critical units of work

Legacy systems are organized by **programs, jobs, and transactions**, not OO classes.
Significant units in this codebase:

{{KEY_PROGRAMS_LIST}}

See [`docs/programs/`](docs/programs/) for one document per significant program.

## Hard constraints

- This system is in **{{LIFECYCLE_PHASE}}** lifecycle phase
  (active development | maintenance-only | migrating | end-of-life).
- **Senior engineers retiring**: {{RETIREMENT_HORIZON}}.
- ADR archaeology is **time-bounded** — capture rationale from
  current senior engineers before they retire.
- Modernization status: {{MODERNIZATION_STATUS}}.

## Code conventions

- Naming: {{NAMING_CONVENTION}}  (e.g., 8.3 PDS member names, ABAP namespace `Z*` / `Y*`)
- Copybooks / shared structures live in: {{COPYBOOK_LOCATION}}
- Comment style: {{COMMENT_STYLE}}

## Build / transport commands

```
{{BUILD_COMMANDS}}
```

## Data is the architecture

Legacy systems are **data-centric**. The most important documentation
section for this codebase is [`docs/data/`](docs/data/):

- [`docs/data/schemas/`](docs/data/schemas/) — DDL, copybooks, DDS, DDIC
- [`docs/data/copybooks-catalog.md`](docs/data/copybooks-catalog.md) — shared structures
- [`docs/data/lineage/`](docs/data/lineage/) — what data flows where

## Gotchas worth knowing

- 🔴 GAP — fill from senior-engineer interviews:
  - Why was {{LEGACY_LANGUAGE}} chosen over alternatives?
  - What incidents shaped current design (the famous outage of YYYY)?
  - Which programs are "do not touch" without breaking production?
  - Which interfaces have undocumented quirks downstream consumers depend on?

## Where decisions live

- ADR archaeology: [`docs/investigations/adr-archaeology/`](docs/investigations/adr-archaeology/)
- ADRs (formal): [`docs/architecture/decisions/`](docs/architecture/decisions/)
- Runbooks: [`docs/runbooks/`](docs/runbooks/)
- Interfaces: [`docs/interfaces/`](docs/interfaces/)
- Modernization plan: [`docs/modernization/`](docs/modernization/) (if migrating)

## Skills available

{{SKILLS_LIST}}

Located in [`.claude/skills/`](.claude/skills/).
