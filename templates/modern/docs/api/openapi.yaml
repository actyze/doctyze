openapi: 3.0.3

# Confidence: 🟡 INFERRED. Doctyze drafted this seed from detected
# endpoints. Verify every path/schema and replace TODO markers.

info:
  title: "{{SERVICE_NAME}} API"
  version: "0.0.1"
  description: "{{ONE_LINE_DESCRIPTION}}"
  license:
    name: "{{LICENSE}}"

servers:
  - url: "https://api.example.com/v1"
    description: "Production"
  - url: "http://localhost:{{LOCAL_PORT}}/v1"
    description: "Local development"

paths:
  /health:
    get:
      summary: Liveness probe
      responses:
        '200':
          description: Service is alive

  # TODO: each detected endpoint should appear here with request/response
  # schemas. Doctyze populates these from extractors/modern/{stack}/.

components:
  schemas: {}
  securitySchemes: {}
