# {{SERVICE_NAME}} — Architecture overview

> Confidence: 🟡 INFERRED unless noted. Generated by Doctyze; verify and update.

## Purpose

{{ONE_PARAGRAPH_PURPOSE}}

## System context

The high-level view: who uses this service, what external systems it touches.

```mermaid
{{SYSTEM_CONTEXT_MERMAID}}
```

See [`diagrams/system-context.mmd`](diagrams/system-context.mmd) for source.

## Container view

Internal building blocks (web app, API service, database, etc.).

```mermaid
{{CONTAINER_VIEW_MERMAID}}
```

See [`diagrams/container.mmd`](diagrams/container.mmd) for source.

## Key sequences

Critical request flows, one diagram per flow.

- [`diagrams/sequence-{{CRITICAL_FLOW_1}}.mmd`](diagrams/)

## Deployment

{{DEPLOYMENT_DESCRIPTION}}

## Source of truth

The canonical architectural model lives in [`workspace.dsl`](workspace.dsl)
(Structurizr C4 DSL). The Mermaid diagrams above are derived views for
inline rendering on GitHub. To regenerate them after editing the DSL:

```bash
doctyze diagrams render
```

## Related

- [Architecture Decision Records](decisions/) — what we chose and why
- [`workspace.dsl`](workspace.dsl) — Structurizr C4 source
