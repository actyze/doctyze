"""Doctyze — generate and maintain a documentation context layer for any repo.

Doctyze brings the playbook; the developer's existing IDE/CI agent brings the LLM.
This package contains only deterministic mechanics (doc discovery, consolidation,
drift detection, distribution) and an MCP server — it never calls an LLM itself.
"""

__version__ = "0.3.1"
